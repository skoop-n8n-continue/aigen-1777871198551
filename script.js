/**
 * Hello World App - Skoop Agent
 * Simple script for digital signage initialization
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Hello World app initialized');

    // Optional: Add a subtle animation or dynamic content update here
    // For now, we keep it simple as requested
});
